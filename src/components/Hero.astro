---
import { Image } from "astro:assets";
import heroImage from "../assets/hero.png";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<section
  id="hero"
  class="relative h-full flex items-center justify-center text-center text-white"
>
  <div class="absolute inset-0 bg-cover bg-center bg-no-repeat">
    <Image
      src={heroImage}
      alt="Hero Image"
      class="h-full w-full object-cover"
      loading="eager"
      widths={widths}
      sizes={sizes}
    />
  </div>
</section>
<div class="absolute inset-0 bg-black/60"></div>
<div class="relative z-10 px-4">
  <h1
    class="text-5xl md:text-7xl lg:text-8xl font-bold mb-4 animate-fade-in-down"
    style={{ textShadow: "2px 2px 8px rgba(0,0,0,0.7)" }}
  >
    Reality is a Puzzle. Can You Solve It?
  </h1>
  <p
    class="text-lg md:text-xl max-w-3xl mx-auto mb-8 animate-fade-in-up"
    style={{ textShadow: "1px 1px 4px rgba(0,0,0,0.7)" }}
  >
    60 minutes. A locked door. A world that defies logic. Welcome to The Paradox
    Box.
  </p>
</div>
